// Custom progress bar widget for torrent list rows.

import { Tokens } from "../tokens.slint";
import { StatusDot } from "../atoms/status_dot.slint";

export component TorrentProgressBar {
    accessible-role: progress-indicator;
    accessible-label: root.progress-text;
    in property <float> progress: 0.0;
    in property <color> fill-color: Tokens.status-seeding;
    in property <string> progress-text: "";

    height: Tokens.sp-6 - Tokens.sp-1;

    // Track
    Rectangle {
        background: Tokens.bg-2;
        border-radius: Tokens.r-sm;

        // Fill
        Rectangle {
            x: 0;
            y: 0;
            width: parent.width * clamp(root.progress, 0.0, 1.0);
            height: parent.height;
            background: root.fill-color;
            border-radius: Tokens.r-sm;
        }

        // Percentage text overlay
        Text {
            text: root.progress-text;
            color: Tokens.fg-0;
            font-size: Tokens.fs-10;
            horizontal-alignment: center;
            vertical-alignment: center;
        }

        // State indicator dot on the trailing edge (same colour as the fill).
        StatusDot {
            x: parent.width - self.width - Tokens.sp-1;
            y: (parent.height - self.height) / 2;
            dot-color: root.fill-color;
            pulsing: false;
        }
    }
}
